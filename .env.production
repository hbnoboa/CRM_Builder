# ===========================================
# CRM Builder - Production Environment
# VM: Google Cloud n2-standard-2
# ===========================================

# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CrmBuilder2026SecurePass
POSTGRES_DB=crm_builder

# Redis
REDIS_PASSWORD=RedisSecure2026Pass

# JWT (mantenha seguro!)
JWT_SECRET=CrmBuilder2026SuperSecretKeyForJWTTokenGenerationPleaseChangeInProduction64chars
JWT_REFRESH_SECRET=CrmBuilder2026RefreshSecretKeyForJWTRefreshTokenGeneration64charsLong
JWT_EXPIRES_IN=7d

# CORS - aceitar de qualquer origem por enquanto (ajuste depois com seu domínio)
CORS_ORIGIN=*

# API URL pública (usar path relativo para funcionar em qualquer host)
NEXT_PUBLIC_API_URL=/api/v1

# Node Environment
NODE_ENV=production
